TP2 - Cartes depuis JSON

Application React Native qui charge des cartes depuis `data/cards.json`.
Fichiers principaux : App.js, components/ (composants réutilisables), data/ (JSON).

But : démontrer le rendu d'une liste de cartes à partir de données locales.
